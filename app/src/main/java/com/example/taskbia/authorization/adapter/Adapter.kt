package com.example.taskbia.authorization.adapter

import android.view.LayoutInflater
import android.view.View
import android.view.ViewGroup
import androidx.recyclerview.widget.RecyclerView
import com.example.taskbia.databinding.ItemResBinding

/*
class Adapter(
    private val listOfLists: MutableList<Task>
)
    : RecyclerView.Adapter<Adapter.ShopItemViewHolder>(), View.OnClickListener {


    override fun onCreateViewHolder(parent: ViewGroup, viewType: Int): ShopItemViewHolder {

        val inflater = LayoutInflater.from(parent.context)
        val binding: ItemResBinding = ItemResBinding.inflate(
            inflater,
            parent,
            false
        )


        return ShopItemViewHolder(binding)
    }

    override fun onBindViewHolder(holder: ShopItemViewHolder, position: Int) {
        val shopList = listOfLists[position]
        with(holder.binding) {


        }
    }

    override fun getItemCount(): Int {
        return listOfLists.size
    }

    class ShopItemViewHolder(val binding: ItemResBinding) :
        RecyclerView.ViewHolder(binding.root)
}*/
